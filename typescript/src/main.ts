import { Game } from 'game';


(async () => {
    const game: Game = new Game();
    game.play();
    // const sc: Screen = new Screen();
    // const stream = await Webcam.getMediaStream();
    // await sc.showDebugCamView(stream);
})();